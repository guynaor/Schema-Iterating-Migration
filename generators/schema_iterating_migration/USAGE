Description:
    This generates a migration file for multi-tenant/multi-schema based application.
    Use this generator insstead of the usual script/generate migration add_fieldname_to_tablename fieldname:string
    The migration file will be created in db/migrate/schema_iterating_migration instead of the usual db/migrate.

Example:
    ./script/generate schema_iterating_migration add_fieldname_to_tablename fieldname:string

    This will create:
		db/migrate/schema_iterating_migration/xxxxxxxxxxxxxx_add_fieldname_to_tablename.rb
